<div>

    {{#with main}}
    <main>
        {{> title data=title big=true}}
        <section>
            <ul>
                {{#each educations}}
                <li>{{name}}
                    {{#unless isEnd}} (щє в процесі) {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>
        {{> title data='My certificates' big=false}}
        <section>
            <ul>
                {{#each certificates}}
                <li>{{name}} ({{id}})
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>